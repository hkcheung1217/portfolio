# Portofolio/email-backend

To start:

    - Use cmd navigate to "portfolio-api" directory
    - Enter "npm install" or "npm i" to install dependencies
    - Enter "npm start"
    - The application will launch on http://localhost:4000/ 
